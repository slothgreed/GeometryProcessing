# HalfEdgeEditor エージェントガイド

`HalfEdgeEditor` の手書き TypeScript / TSX / JavaScript / JSX を作成、変更、レビューするときは、
`skills/halfedge-editor-style/SKILL.md` を最初に読み、その規約に従うこと。

生成物である `node_modules/`、`dist/`、`.next/`、`.vinext/`、`.vs/` は編集しないこと。
C++ 側のコード規約には、このディレクトリの規約を適用しないこと。

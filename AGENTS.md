# PointCloudApp エージェントガイド

## プロジェクトの目的

このリポジトリは、幾何処理、リアルタイムグラフィックス、AIを研究・学習するための個人用PoCである。
実験の速度と、実際に動作する結果を優先すること。
ユーザーから明示的な依頼がない限り、製品規模の設計、パッケージ化、過剰な抽象化を導入しないこと。

## リポジトリ構成

- `PointCloudApp/`: Windows向けのメインC++アプリケーション。OpenGL描画、幾何アルゴリズム、ファイルローダー、シーンノード、UI、シェーダーを含む。
- `GeometryAI/`: AI処理を担当するLibTorchベースの別実行ファイル。`ServerPipe`でコマンドを受け取り、`ICommandReceiver`の派生クラスで処理する。
- `extlibs/`: サードパーティライブラリ。明示的に必要とされない限り、外部ライブラリのソースを編集しないこと。
- `resource/`: 実行時のアセットとサンプルデータ。明示的な許可なく書き換えたり削除したりしないこと。
- `ReadMeResource/`: `ReadMe.md`で使用する画像とアニメーション。

## ビルド環境

- 主な環境はWindows、Visual Studio 2022、MSVC v143、x64。
- メインコードはC++20を使用する。
- ソリューションのエントリーポイントは`PointCloudApp.sln`。
- `.vcxproj`内の一部の依存関係には、この開発環境固有の絶対パスが使われている。無関係な作業のついでに、これらのパスを置換または正規化しないこと。
- 主な依存関係はOpenGL、GLFW、GLEW、ImGui、nvpro_core、CUDA、LibTorch、CGAL、TBB、PCL、Eigen、GLM、Microsoft glTF SDK。
- 原則としてx64でビルドする。Visual Studio、MSBuild、GPU SDK、ローカル依存関係が不足してビルドできない場合は、その理由を明確に報告すること。

## 変更方針

- 変更範囲は、依頼された実験または機能に限定すること。
- 動作変更を明示的に求められていない限り、既存の動作を維持すること。
- 無関係な大規模近代化、フォーマット変更、名前変更、ディレクトリ再編を行わないこと。
- 現代的なC++の慣習と異なる場合でも、基本的には周囲のファイルのスタイルに合わせること。
- `.vs/`、`x64/`、`Debug/`、`Release/`以下の生成物を編集しないこと。
- アプリケーション側の不具合を回避する目的で`extlibs/`以下を変更する場合は、その必要性を説明すること。
- バイナリ形式とGPUバッファレイアウトは互換性境界として扱うこと。変更前にフィールド順序、サイズ、アライメント、要素数を確認すること。

## GeometryAIの規約

- AIコマンドハンドラーは`KI::ICommandReceiver`を継承する。
- 新しいハンドラーには通常、次の変更が必要になる。
  1. `<Feature>AI.h`と`<Feature>AI.cpp`を追加する。
  2. `GetCommandName()`から一意なコマンド名を返す。
  3. 名前付きパイプ経由で使用する場合は、`GeometryAI/main.cpp`で`ServerPipe::SetReceiveCommand`に登録する。
  4. `GeometryAI.vcxproj`と`GeometryAI.vcxproj.filters`の両方にファイルを登録する。
- パースしたコマンドトークンを添字で参照する前に、引数数を検証すること。
- モデル定義、学習、推論、シリアライズ、データ形式の前提は、対象機能のファイル内で確認できるようにすること。
- 要件が曖昧なときに、学習データ形式、テンソル形状、ノイズスケジュール、モデル構造を勝手に決めないこと。コンパイル可能な雛形を作るか、実装を左右する不足情報をユーザーへ確認すること。
- 推論時は`torch::NoGradGuard`を使い、実際の推論を実装するときはCPUとGPU間のデバイス転送を明示すること。

## PointCloudAppの規約

- OpenGLリソースの生成と破棄を対応させること。
- シェーダーコードを変更したときは、コンパイルエラーとリンクエラーを確認すること。
- 明示的な変更依頼がない限り、座標系、頂点の巻き順、単位の前提を維持すること。
- `.cpp`または`.h`を追加した場合は、`PointCloudApp.vcxproj`と`PointCloudApp.vcxproj.filters`にも登録すること。
- `GeometryAI`へコマンドを送る場合は、コマンド名と引数順を対応する`ICommandReceiver`実装と一致させること。

## 検証

- 最低限、変更したクラスやコマンドへの参照を`rg`で確認すること。
- MSBuildとローカル依存関係が利用できる場合は、影響を受ける最小のVisual Studioプロジェクトをビルドすること。
- CPU上の幾何処理では、小さく決定的な入力を優先し、要素数、境界、トポロジー、数値が有限であることを確認すること。
- 描画やGPU処理について実行時または目視確認ができなかった場合は、その旨を明記すること。
- 実際に実行していないビルドやテストを成功したと報告しないこと。

## コミュニケーション

- ユーザーとのやり取りは、特に指定がない限り日本語で行うこと。
- 実装済みの処理、雛形、未実装のTODOを明確に区別すること。
- グラフィックスやAIで有効な方式が複数ある場合は、判断に必要な違いを簡潔に説明し、実装内容を大きく左右する選択だけを確認すること。
